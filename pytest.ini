[pytest]
asyncio_mode = auto
# Repo root on sys.path for the HA integration tests (custom_components.*);
# the integration package dir for the pure modules (api, bucketing), which
# import directly without importing the HA __init__.py.
pythonpath = . custom_components/wrm_water
testpaths = tests
